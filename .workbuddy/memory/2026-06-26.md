# 2026-06-26 工作日志

## 撰写 OpenSpec 项目规范文档

- 审查了项目所有文件：`Windows_UserFiles_Mover.py`、`Windows_UserFiles_Mover.bat`、`Move-DevConfigs.ps1`、`README.md`、`TODO.md`、`VERSION_HISTORY.md` 及 `docs/` 下所有文档。
- 新建 `docs/openspec.md`，包含 12 个章节：
  - 项目概述（定位、目标用户、核心约束）
  - 架构设计（分层架构图、版本矩阵、核心技术）
  - 文件结构规范（目录布局、命名规范、临时文件约定）
  - 功能模块规范（迁移流程、ROBOCOPY 参数、回滚、开始菜单备份/还原）
  - 编码规范（Python PEP 8、批处理约定、PowerShell 约定）
  - 错误处理规范（分级、权限检查、参数验证）
  - 日志规范（格式、输出策略、Python 双输出实现）
  - 版本管理规范（语义版本号、版本号同步位置、提交信息格式）
  - 文档规范（文档清单、Markdown 规范、中文写作风格）
  - 测试规范（分类、环境要求、手动检查清单）
  - 发布规范（发布前检查、EXE 打包、Git 提交格式）
  - 贡献规范（开发环境、PR 流程、Issue 规范、禁止提交内容）
- 更新 `README.md` 的文档目录，将 `6A工作流规则.md`（已不存在）替换为 `docs/openspec.md` 的链接。
